'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

/*
 * Returns a path to the original variable if the current one is a circular reference
 *
 * @param {any} obj
 * @param {object} cache
 * @param {array} currentPath
 * @returns {Array}
 */
var getPathToCircularRef = function getPathToCircularRef(obj, cache, currentPath) {
  var circPath = [];

  if ((typeof obj === 'undefined' ? 'undefined' : _typeof(obj)) !== 'object') {
    return circPath;
  }

  var pos = cache.objects.indexOf(obj);
  if (pos >= 0) {
    circPath = cache.paths[pos];
  }

  cache.objects.push(obj);
  cache.paths.push(currentPath);

  return circPath;
};
exports.default = getPathToCircularRef;